$ ../emacs-22.3/configure --prefix=/usr --libexecdir=/usr/lib --localstatedir=/var
$ make bootstrap
$ sudo make install
$ sudo chown -v -R root:root /usr/share/emacs/22.3/
./configure --prefix=/usr --without-x --localedir=/usr/share/locale --localstatedir=/var --sysconfdir=/etc
CFLAGS='-g -Wno-pointer-sign' ./configure --prefix=/usr --without-x --localedir=/usr/share/locale --localstatedir=/var --sysconfdir=/etc
CFLAGS='-g -Wno-pointer-sign' ./configure --prefix=/usr --without-x --localedir=/usr/share/locale --localstatedir=/var --sysconfdir=/etc --libexecdir=/usr/lib

** error
$ emacs
emacs: Cannot open termcap database file
compile and install ncurse, then compile and install emacs
